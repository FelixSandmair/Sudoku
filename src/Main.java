import java.awt.*;


public class Main {
    public static void main(String[] args) {
        //Sudoku s = new Sudoku(0, colors[0]);
        SudokuLauncher sudokuLauncher = new SudokuLauncher();
        //SudokuGenerator s;
        //for(int i = 0; i < 200; i++) {
        //    s = new SudokuGenerator(Difficulty.EASY);
        //}
        //Solver s = new Solver();
        /*SudokuGenerator sgen;
        for(int i = 0; i < 200; i++) {
            sgen = new SudokuGenerator(Difficulty.EASY);
            SudokuGenerator.printGrid(sgen.getGridRows());
            System.out.println("Sudoku number " + (i + 1) + " is solved " + SudokuGenerator.isPerfect(sgen.getGridRows()));
        }*/
    }
}